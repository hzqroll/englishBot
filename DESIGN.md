# EnglishBot DESIGN.md

## 文档目的

这份文档是当前版本的设计真源，专门约束 `EnglishBot` 在飞书中的学习体验。

它解决的不是“页面好不好看”，而是下面 4 个更根本的问题：

- 卡片有没有明确的阶段感
- 用户能不能在 3 秒内知道下一步做什么
- 双人学习关系有没有被清楚地设计出来
- 产品是不是还在无意识地退回“命令式英语 bot”

这份文档默认覆盖当前主线产品，不再以旧的“群学习训练营 + QQ/飞书双渠道 + 命令驱动”作为设计基线。

## 产品立场

### 产品定义

`EnglishBot` 当前在用户面前的产品定义不是“英语机器人”，而是：

`B2 Sprint OS`

它是一个运行在飞书群里的双人学习系统，目标不是提供更多内容，而是每天把两个人推进下一个最小学习动作。

### 设计主题

设计主题固定为：

`Warm Discipline`

含义如下：

- 有温度，但不卖萌
- 有教练感，但不说教
- 有节奏，但不做报表后台
- 有仪式感，但不堆视觉装饰

### 核心体验原则

后续所有卡片与文案必须服从这 5 条原则：

1. 执行优先
   用户看到卡片后应该立刻知道怎么做，而不是先读一堆内容。

2. 双人关系可见
   A 发起、B 接棒、明天交换，必须在界面中心而不是边角信息。

3. 阶段感明确
   晨间卡、提醒卡、晚间卡、周卡必须一眼看出用途不同，不能只是同一模板换标题。

4. 文案像教练，不像命令帮助页
   默认鼓励直接行动，不把用户拉回“发送 今日任务 / 复习一下”。

5. 学习仪式重于统计完整性
   卡片的目标是推进动作，不是展示系统知道多少数据。

## Autoplan 决策

这次针对 design-review 暴露的问题，自动收敛为以下决策，不再悬而未决：

1. `daily_session` 不再继续复用通用卡片语法，必须拥有自己的执行卡语法。
2. 晚间进度卡必须瘦身，默认最多 3 个主区块。
3. 提醒卡统一改成中文主标题，不再混用 `Midday Baton` / `Evening Baton`。
4. 周报页脚不再回退到命令提示。
5. `B2 Sprint` 作为系统标签保留，但内容标题采用中文主导，减少中英混搭。
6. 通用 renderer 继续保留，但不再作为主线学习卡片的默认体验。

## 卡片系统

主线产品固定使用 5 类卡片。

### 1. Morning Execution

#### 目的

晨间开局。用户看到卡片后立刻开始 A -> B 的学习闭环。

#### 标题规则

- 标题：`Day MM/DD | 今日主题`
- 系统标签：`B2 Sprint`
- 状态标签：只允许出现这些短标签
  - `恢复模式`
  - `语音日`
  - `Benchmark`

#### 首屏必须出现的信息

首屏只允许出现这 4 组信息：

- `今日目标`
- `今日角色`
- `必须复用`
- `今天动作`

`完成标准` 可以保留，但必须放在动作之后，且不超过 3 行。

#### 允许的内容

- 1 句目标
- A/B 角色与交换提示
- 2-3 个必须复用表达
- 3 步动作
- 2-3 条完成标准

#### 禁止出现在首屏的内容

- 长 transcript
- 解释型段落
- 太多支持词汇
- 数据统计
- 命令提示

#### 文案范式

- `今天练一轮真实工作场景互动。`
- `A 先发 2-3 句，B 负责追问或补充。`
- `直接在群里开始，不需要先发命令。`

### 2. Baton Reminder

#### 目的

只做一件事：把接力棒推到当前该行动的人手里。

#### 标题规则

- 中午：`中午接棒提醒`
- 晚上：`晚间接棒提醒`

不再使用 `Midday Baton` / `Evening Baton`。

#### 内容结构

提醒卡固定只保留 1 个主区块：

- `现在该做什么`

区块内固定只放：

- 当前轮到谁
- 一句原因
- 一句最小动作
- 最多 2 个必须复用表达

#### 语气规则

- 直接
- 短句
- 有点压迫感，但不责备

示例：

- `A：还没看到今天的第一棒。现在先发 2-3 句英文。`
- `B：A 已经发起，轮到你接棒追问或补充。`

### 3. Evening Progress

#### 目的

告诉用户“今天到了哪”，而不是展示一整份学习数据看板。

#### 标题规则

- 标题：`今晚进展`

#### 内容结构

晚间进度卡最多 3 个主区块：

1. `今天到了哪`
   - 当前 session 状态
   - A/B 当前关系

2. `学习证据`
   - 今天最有代表性的 1-2 个表达
   - 如无表达，用一句解释代替

3. `下一步`
   - 今晚还需要做什么
   - 明天会发生什么

#### 必须删除的内容

下面这些信息不应再出现在主卡片主体：

- 翻译次数
- 纠错次数
- 今日积分
- 连续学习天数
- 周测状态
- 过细的 evidence score
- 大段掌握度解释

这些内容可以保留到：

- 管理后台
- 周报
- 明文文本 fallback
- 后续折叠区

但不应继续占据主视觉层。

### 4. Evening Repair

#### 目的

这是“今晚修什么”的教练卡，不是错误数据库导出。

#### 标题规则

- 标题：`今晚修正`

#### 内容结构

固定 3 个区块：

1. `今天最该修的`
   - 最多 3 个错误

2. `正确说法`
   - 与上面一一对应

3. `明天回捞`
   - 1-2 个必须复用的表达或句型

#### 文案要求

- 只挑最重要的 1-3 个问题
- 不做“全量错误展示”
- 默认下一步是“立刻重说或重写一轮”

#### 页脚规则

页脚可以提醒回捞，但不能写成命令说明书。

允许：

- `今晚修正这一轮，明天继续带着它们开口。`

不允许：

- `发送 复习一下 查看更多内容`

### 5. Weekly Checkpoint

#### 目的

每周 checkpoint 不是“数据结算单”，而是“阶段复盘 + 下周方向”。

#### 标题规则

- 标题：`本周复盘`

#### 内容结构

固定 4 个区块：

1. `本周推进`
   - 本周学了几天
   - 完成度
   - 主要主题

2. `这周暴露出来的边界`
   - 2-3 个最真实的问题

3. `这周已经长出来的能力`
   - 1-3 个正向变化

4. `下周主攻`
   - 一个明确焦点

#### 页脚规则

周卡页脚不再提示命令。

统一改成类似：

- `下周继续沿着同一条主线推进。`

## 信息层级规则

### 一级信息

一级信息是用户在 3 秒内必须看懂的内容，只允许包括：

- 今天练什么
- 谁先开始
- 现在轮到谁
- 今晚修什么
- 明天带什么走

### 二级信息

二级信息可以在用户继续往下看时出现：

- 必须复用表达
- 典型例句
- 1-2 个代表性证据

### 三级信息

三级信息不该默认出现在主视图：

- 统计指标
- 模型总结长文
- 历史回捞细节
- 长篇解释

## 命名规则

### 系统命名

用户可见系统标签统一使用：

- `B2 Sprint`

### 标题命名

卡片标题统一采用中文主导：

- `Day 04/13 | 项目延期说明`
- `中午接棒提醒`
- `晚间接棒提醒`
- `今晚进展`
- `今晚修正`
- `本周复盘`

不再混用英文标题和中文内容标题。

## 文案规则

### 语气

语气必须满足：

- 短句
- 动作导向
- 少解释
- 不卖萌
- 不官腔

### 常用句式

优先使用：

- `现在先做这一轮。`
- `直接在群里开始。`
- `今晚只修最重要的几个点。`
- `明天继续带着这些表达开口。`

避免使用：

- `请继续保持学习热情`
- `你可以发送某某命令`
- `系统已为你生成以下数据`

## 视觉语法规则

### 通用要求

- 不要在每个区块前都插一条同质化分割线
- 不同卡片类型必须有不同的布局节奏
- 执行卡要有明显的“主任务块”
- 提醒卡要像单点推动，不像缩略日报
- 晚间卡要更收束、更安静

### 具体要求

1. `Morning Execution`
   - 强主区块
   - 清楚的动作顺序
   - 角色信息必须靠前

2. `Baton Reminder`
   - 体积最小
   - 单核心信息
   - 不要塞第二层分析

3. `Evening Progress`
   - 轻总结
   - 轻证据
   - 强下一步

4. `Evening Repair`
   - 强聚焦
   - 少而准
   - 明确明日回捞

5. `Weekly Checkpoint`
   - 更像阶段复盘
   - 少运营感
   - 少命令感

## 当前实现的设计改造要求

以下是直接映射到当前代码的设计要求。

### 1. 渲染器分流

当前只有 `daily_lesson` 走专用 renderer，`daily_session / session_reminder / progress / error_digest` 仍走通用 renderer。

后续应改成至少 5 类专用渲染：

- `daily_session`
- `session_reminder`
- `progress`
- `error_digest`
- `weekly_report`

通用 renderer 继续保留给非主线卡片。

### 2. 执行卡不再沿用“section + hr 平铺”

当前通用 renderer 会把所有区块压成同一层级。

后续执行卡需要有专门布局，至少支持：

- 顶部状态标签
- 强主任务区
- 角色区
- 动作区
- 完成标准区

### 3. 晚间卡瘦身

`今晚进展` 和 `今晚修正` 都要压缩。

具体目标：

- `今晚进展` 不超过 3 个区块
- `今晚修正` 不超过 3 个错误

### 4. 页脚去命令化

任何主线卡片都不再默认提示：

- `发送 今日任务`
- `发送 复习一下`

如果需要保留命令入口，只能放在帮助页或兜底场景，不进入主卡片默认文案。

## 编码落地顺序

### Phase 1

- 给 `daily_session` 单独 renderer
- 给 `session_reminder` 单独 renderer
- 把标题统一成中文主标题

### Phase 2

- 重写 `progress` 卡片结构
- 重写 `error_digest` 卡片结构
- 去掉周报页脚的命令回退

### Phase 3

- 如有必要，扩展 `CardDocument` 结构以支持：
  - 状态标签
  - hero block
  - compact metrics
  - collapsible secondary info

## 验收标准

设计验收不看“字段有没有”，看“用户能不能更快行动”。

以下 6 条全部满足，才算设计落地成功：

1. 晨间卡打开后 3 秒内能知道今天要做什么。
2. 用户不用读很长内容就知道谁先开始。
3. 提醒卡看起来像催行动，不像日报。
4. 晚间卡优先推动修正，不优先展示统计。
5. 周卡不再把用户拉回命令式入口。
6. 整套卡片看起来像一个学习系统，而不是一组 bot 回复模板。

## 与旧文档关系

旧的 [docs/product-design.md](docs/product-design.md) 仍可作为产品背景文档保留。

但从现在开始，飞书主线学习体验、卡片系统、文案语气和阶段语法，以本文件为准。
